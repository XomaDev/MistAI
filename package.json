{
  "name": "MistAI",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && esbuild build/index.js --bundle --platform=browser --outfile=dist/mistai.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.5.3"
  },
  "private": true
}
